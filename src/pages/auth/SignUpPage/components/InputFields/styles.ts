import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles(() => ({
	container: {
		maxWidth: 376,
	},
	userDataContainer: {
		maxWidth: 180,
	},
	personalDataContainer: {
		marginTop: 16,
	},
}));
