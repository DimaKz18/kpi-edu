import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles(() => ({
	container: {
		maxWidth: 276,
	},
	userDataContainer: {
		maxWidth: 134,
	},
	personalDataContainer: {
		marginTop: 16,
	},
}));
